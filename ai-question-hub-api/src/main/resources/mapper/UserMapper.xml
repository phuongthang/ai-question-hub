<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiquestionhub.aiquestionhubapi.api.repository.UserRepository">

    <resultMap id="UserResultMap" type="com.aiquestionhub.aiquestionhubapi.api.model.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="userCode" column="user_code"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="fullName" column="full_name"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="createdAt" column="created_at"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="deletedAt" column="deleted_at"/>
        <result property="deletedBy" column="deleted_by"/>
    </resultMap>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users WHERE deleted_at IS NULL
    </select>

    <select id="findById" parameterType="long" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username} AND deleted_at IS NULL
    </select>

    <select id="findByEmail" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="existsByUsername" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByCode" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_code = #{code} AND deleted_at IS NULL
    </select>

    <insert id="insert" parameterType="com.aiquestionhub.aiquestionhubapi.api.model.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, user_code, email, password, full_name, avatar_url, phone_number, created_at, created_by)
        VALUES (#{username}, #{userCode}, #{email}, #{password}, #{fullName}, #{avatarUrl}, #{phoneNumber}, CURRENT_TIMESTAMP, #{createdBy})
    </insert>

    <update id="update" parameterType="com.aiquestionhub.aiquestionhubapi.api.model.User">
        UPDATE users
        SET username     = #{username},
            email        = #{email},
            password     = #{password},
            full_name    = #{fullName},
            avatar_url   = #{avatarUrl},
            phone_number = #{phoneNumber},
            updated_at   = CURRENT_TIMESTAMP,
            updated_by   = #{updatedBy}
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

    <update id="deleteById" parameterType="long">
        UPDATE users
        SET deleted_at = NOW()
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

</mapper>
